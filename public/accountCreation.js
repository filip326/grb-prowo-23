/** @type {HTMLTextAreaElement} */
const textarea = document.querySelector('textarea#users');

/** @type {HTMLFormElement} */
const form = document.querySelector('form');

form.addEventListener('submit', (e) => {
    e.preventDefault();

    

})